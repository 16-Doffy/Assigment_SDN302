<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/styles.css">
  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; img-src 'self' data: https:; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.tailwindcss.com; style-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; font-src 'self' https://cdnjs.cloudflare.com; connect-src 'self'">
</head>
<body class="min-h-screen bg-gray-50">
  <%- include('../partials/header-public') %>

  <div class="max-w-6xl mx-auto px-4 md:px-6 lg:px-8 py-10">
    <div class="grid md:grid-cols-2 gap-8 items-center">
      <!-- Left: Banner -->
      <div>
        <%- include('../partials/banner', { imageUrl: '/img/nduy6.png', altText: 'Perfume campaign', ctaText: 'Khám phá ngay', ctaHref: '/products' }) %>
      </div>

      <!-- Right: Register Card -->
      <div class="bg-white shadow-md rounded-2xl p-6 md:p-8 border border-gray-100">
        <h1 class="text-2xl md:text-3xl font-bold text-gray-900 text-center">Create account</h1>
        <% if (error) { %>
          <div class="mt-4 rounded-md bg-red-50 p-3 text-red-700 text-sm"><%= error %></div>
        <% } %>

        <form class="mt-6 space-y-6" action="/auth/register" method="post">
          <div>
            <label class="block text-sm font-semibold text-gray-700 mb-2">Username</label>
            <input 
              class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:ring-2 focus:ring-purple-500 focus:border-purple-500 transition-all duration-200 placeholder-gray-400 text-gray-700 bg-gray-50 hover:bg-white focus:bg-white" 
              type="text" 
              name="username" 
              value="<%= formData && formData.username ? formData.username : '' %>" 
              placeholder="Nhập tên đăng nhập"
              required 
            />
          </div>
          <div>
            <label class="block text-sm font-semibold text-gray-700 mb-2">Password</label>
            <input 
              class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:ring-2 focus:ring-purple-500 focus:border-purple-500 transition-all duration-200 placeholder-gray-400 text-gray-700 bg-gray-50 hover:bg-white focus:bg-white" 
              type="password" 
              name="password" 
              placeholder="Nhập mật khẩu (tối thiểu 6 ký tự)"
              required 
              minlength="6" 
            />
          </div>
          <div>
            <label class="block text-sm font-semibold text-gray-700 mb-2">Full name</label>
            <input 
              class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:ring-2 focus:ring-purple-500 focus:border-purple-500 transition-all duration-200 placeholder-gray-400 text-gray-700 bg-gray-50 hover:bg-white focus:bg-white" 
              type="text" 
              name="fullname" 
              value="<%= formData && formData.fullname ? formData.fullname : '' %>" 
              placeholder="Nhập họ và tên"
            />
          </div>
          <div>
            <label class="block text-sm font-semibold text-gray-700 mb-2">Birth year</label>
            <input 
              class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:ring-2 focus:ring-purple-500 focus:border-purple-500 transition-all duration-200 placeholder-gray-400 text-gray-700 bg-gray-50 hover:bg-white focus:bg-white" 
              type="number" 
              name="birthYear" 
              min="1900" 
              max="2100" 
              value="<%= formData && formData.birthYear ? formData.birthYear : '' %>" 
              placeholder="Nhập năm sinh"
            />
          </div>

          <button type="submit" class="w-full inline-flex justify-center items-center rounded-xl bg-gradient-to-r from-purple-600 to-indigo-600 text-white font-semibold py-3 hover:from-purple-700 hover:to-indigo-700 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:ring-offset-2 transition-all duration-200 shadow-lg hover:shadow-xl">Create account</button>
        </form>

        <div class="mt-6 text-sm text-center">
          <span class="text-gray-600">Already have an account?</span>
          <a class="text-indigo-600 hover:underline ml-1" href="/auth/signin">Sign in</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>


